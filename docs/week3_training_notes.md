# Week 3 Training Notes

## Goal

The goal of Week 3 Day 1 was to train the first simple classifier for the SignAssist CV project.

The project moved from data collection to model training.

## Current pipeline

```text
landmark CSV → feature matrix → train/test split → classifier → accuracy result → saved model